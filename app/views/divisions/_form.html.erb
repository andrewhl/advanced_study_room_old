<%= form_for Division.new, :html => {:id => "division_form"} do |f| %>
  <div class="field">
    <%= f.text_field :division_name %>
  </div>
  <div class="actions">
    <%= f.submit :create %>
  </div>
  <span class="ajax_message">
<% end %>