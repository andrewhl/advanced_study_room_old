<%= form_for @bracket, :url => division_brackets_path(division), :html => {:id => "bracket_create_form"} do |f| %>
  <div class="field">
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit :create %>
  </div>
  <span class="ajax_message">
<% end %>


